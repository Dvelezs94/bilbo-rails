.carousel-item{ class: "#{'active' if image_iteration.first?}"}
  = link_to url_from_media(image), class: "glightbox d-block w-100", :data => {group: image.record.id} do
    = image_tag url_from_media(image.variant(resize_to_limit: [600, 600]).processed), class: "img-inherit", alt: "No Image"
