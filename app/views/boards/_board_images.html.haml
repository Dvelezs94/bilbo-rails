.carousel-item{ class: "#{'active' if image_iteration.first?}"}
  = link_to url_from_media(image), class: "js-smartPhoto d-block w-100", :data => {group: image.record.id} do
    = image_tag url_from_media(image), class: "img-inherit", alt: "No Image"
