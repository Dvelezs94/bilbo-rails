<% unless @success %>
  show_error("<%= @campaign.errors.full_messages.first %>");
  $("#customSwitch-" + "<%= @campaign.slug %>").prop('checked', <%= @campaign.state %>);
  console.log(<%= @campaign.state %>)
<% end %>
