$('#content<%=@board_campaigns.board.slug%>').html('<%= j (render "campaigns/display_content", object: { content: @contents, campaign: @campaign, board_campaigns: @board_campaigns }) %>');
initLightbox();
