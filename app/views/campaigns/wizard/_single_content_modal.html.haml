.card.wd-30p{"data-content": "#{content.id}"}
  -if content.is_video?
    = image_tag content.multimedia_url(:small), class: "card-img-top", id: "card-ad-link"
  - else
    -if content.multimedia(:large).height <= 1080
      = image_tag content.multimedia_url(:small), class: "card-img-top", id: "card-ad-link"
    - else
      = image_tag "construction_video.webp", class: "card-img-top"
  .card-body.pd-5-f
    %p.card-text{style: "margin-bottom: 0.5rem;"}= content.multimedia.metadata["filename"].truncate(30)
    %p.card-text
      %small.text-muted= "#{content.multimedia(:large).width.round(0)} x #{content.multimedia(:large).height.round(0)}"

-# .col-sm
-#   -if content.is_video?
-#     = link_to content.multimedia_url(:transcoded), class: "glightbox img-inherit", :data => {caption: content.multimedia.metadata["filename"], group: @project.slug, id: "#{content.id}"} do
-#       .card#card-ad-link{"data-content": "#{content.id}", class: "img wd-100 ht-100 rounded"}
-#         = image_tag content.multimedia_url(:large), class: "img-inherit", :controls => false, :controlslist => "nodownload" , autoplay: false, loop: true, muted: 'muted'
-#   -else
-#     -if content.multimedia(:large).height <= 1080
-#       = link_to content.multimedia_url(:large), class: "glightbox img-inherit", :data => {caption: content.multimedia.metadata["filename"], group: @project.slug, id: "#{content.id}"} do
-#         .card#card-ad-link{"data-content": "#{content.id}", class: "img wd-100 ht-100 rounded"}
-#           = image_tag content.multimedia_url(:small), class: "img-inherit"
-#     -else
-#       = image_tag "construction_video.webp", class: "img-inherit"
-#     -if content.is_video?
-#       %h6.mmtitle= content.multimedia.metadata["filename"].truncate(30)
-#     -else
-#       -if content.multimedia(:large).height <= 1080
-#         %h6.mmtitle= content.multimedia.metadata["filename"].truncate(30)
-#       - else
-#         %h6.mmtitle= I18n.t('ads.processing', filename: content.multimedia.metadata["filename"].truncate(30))
-#     - if content.multimedia(:large).width.nil?
-#       %span= t("ads.measures")
-#     -else
-#       %span= "#{content.multimedia(:large).width.round(0)} x #{content.multimedia(:large).height.round(0)}"
-#     %div.pd-t-10




//.card.wd-30p{"data-content": "#{content.id}"}
//  -if content.is_video?
//    = image_tag content.multimedia_url(:small), class: "card-img-top"
//  - else
//    -if content.multimedia(:large).height <= 1080
//      = image_tag content.multimedia_url(:small), class: "card-img-top"
//    - else
//      = image_tag "construction_video.webp", class: "card-img-top"
//  .card-body.pd-5-f
//    %p.card-text{style: "margin-bottom: 0.5rem;"}= content.multimedia.metadata["filename"].truncate(30)
//    %p.card-text
//      %small.text-muted= "#{content.multimedia(:large).width.round(0)} x #{content.multimedia(:large).height.round(0)}"
