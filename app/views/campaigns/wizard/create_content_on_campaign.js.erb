$("#single_content").append("<%= j render partial: 'campaigns/wizard/single_content_modal', collection: @content_array, as: :content%>");
remove_on_click();
select_content();
initialize_content_filter();
<% if @success_message.present? %>
  show_success("<%= escape_javascript(@success_message)%>");
<% else %>
  show_error("<%= escape_javascript(@error_message)%>");
<% end %>
if($("#modalNewMultimedia").length){
  $('#modalNewMultimedia').modal('hide');
}else if ($('#modalNewUrl').length) {
  $('#modalNewUrl').modal('hide');
}
//$('#modalContent').css("overflow-y", "scroll");
