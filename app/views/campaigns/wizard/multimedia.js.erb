$("#modalContent").append("<%= j (render 'campaigns/wizard/content', locals: {content: @content, board_name: @board_name, slug: @slug}) %>");
$("#modalContent").modal();
$("#content_modal").html("<%= j render partial: 'campaigns/wizard/contents_modal'%>");
select_content();
