- @board.default_images.each do |di|
  - if di.video?
    = video_tag(di, class: "bilbo-official-ad", loop: true, preload: "auto", muted: 'muted')
  - else
    = image_tag di, class: "bilbo-official-ad"
