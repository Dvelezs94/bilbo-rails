<%= render 'shared/header', menu: "ads" %>

<div class="content content-fixed">
  <div class="container pd-x-0 pd-lg-x-10 pd-xl-x-0">
    <div class="d-sm-flex align-items-center justify-content-between mg-b-20 mg-lg-b-30">
      <div>
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb breadcrumb-style1 mg-b-10">
            <li class="breadcrumb-item">
              <a href="/"><%= t("campaign.home") %></a>
            </li>
            <li class="breadcrumb-item active" aria-current="page"><%= t("ads.ads") %></li>
          </ol>
        </nav>

        <h4 class="mg-b-0 tx-spacing--1"><%= t("ads.ads") %></h4>
      </div>
      <div class="d-none d-md-block">
        <a href="#modalNewAd" data-toggle="modal">
          <button class="btn btn-sm pd-x-15 btn-primary btn-uppercase mg-l-5">
            <i class="wd-10 mg-r-5 fe fe-plus"></i>
            <%= t("ads.new_ad") %></button>
        </a>
      </div>
    </div>

    <!-- Content -->
    <div class="card-columns">
      <%= render partial: "new_ad_card" %>
      <%= render partial: 'overview_card', collection: @ads, as: :ad %>
    </div>

  </div>
  <!-- container -->
</div>
<!-- content -->
<%= render partial: "ads/new" %>
