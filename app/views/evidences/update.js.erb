$('#evidences-<%=@evidence.board.slug%>').html("<%= j render partial: 'evidences/show_selected_evidences' %>");
remove_on_click()
hover_img();
if($("#modalNewEvidence").length){
  $('#modalNewEvidence').modal('hide');
}

<% if @success_message.present? %>
  show_success("<%= escape_javascript(@success_message)%>");
<% else %>
  show_error("<%= escape_javascript(@error_message)%>");
<% end %>
